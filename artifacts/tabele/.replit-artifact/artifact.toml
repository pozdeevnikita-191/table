kind = "web"
previewPath = "/"
title = "Tabель — Учёт рабочего времени"
version = "1.0.0"
id = "artifacts/tabele"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20603

[services.development]
run = "pnpm --filter @workspace/tabele run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/tabele", "run", "build" ]
publicDir = "artifacts/tabele/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20603"
BASE_PATH = "/"
